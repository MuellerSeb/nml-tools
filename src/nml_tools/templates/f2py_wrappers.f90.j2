! This file was generated by nml-tools.
!> \file {{ file_name }}
{% for spec in specs %}
!> \copydoc {{ spec.f2py_module_name }}
{% endfor %}

{% for spec in specs %}
!> \brief {{ spec.brief }}
!> \details {{ spec.details_lines[0] }}
{% for line in spec.details_lines[1:] %}
!! {{ line }}
{% endfor %}
module {{ spec.f2py_module_name }}
  use iso_c_binding, only: c_intptr_t
{% if spec.derived_type_names %}
  use {{ spec.helper_module }}, only: NML_OK, NML_ERR_INVALID_INDEX
{% endif %}
{% if spec.kind_imports %}
  use {{ spec.kind_module }}, only: &
    {{ spec.kind_imports | join(', &\n    ') }}
{% endif %}
  use {{ spec.module_name }}, only: {{ spec.type_name }}, {{ spec.resolve_handle_name }}{% for type_name in spec.derived_type_names %}, {{ type_name }}{% endfor %}

  implicit none

contains

  !> \brief Read the {{ spec.namelist_name }} namelist from a file
  subroutine {{ spec.namelist_name }}_from_file_wrapper(nml__handle, file, nml__status, nml__errmsg)
    integer(c_intptr_t), intent(in) :: nml__handle !< opaque handle to a {{ spec.type_name }} instance
    character(len=*), intent(in) :: file !< namelist file path
    integer, intent(out) :: nml__status !< nml-tools status code
    character(len={{ spec.errmsg_len }}), intent(out) :: nml__errmsg !< error message for non-OK status values
    type({{ spec.type_name }}), pointer :: nml__obj

    call {{ spec.resolve_handle_name }}(nml__handle, nml__obj, nml__status, nml__errmsg)
    if (.not. associated(nml__obj)) then
      return
    end if
    nml__status = nml__obj%from_file(file, errmsg=nml__errmsg)
  end subroutine {{ spec.namelist_name }}_from_file_wrapper

  !> \brief Set {{ spec.namelist_name }} values on the handled instance
  subroutine {{ spec.namelist_name }}_set_wrapper(nml__handle, &
{% if spec.argument_list %}
    {{ spec.argument_list | join(', &\n    ') }}, &
{% endif %}
    nml__status, nml__errmsg)
    integer(c_intptr_t), intent(in) :: nml__handle !< opaque handle to a {{ spec.type_name }} instance
{% for decl in spec.argument_declarations %}
{{ decl | indent(4, True) }}
{% endfor %}
    integer, intent(out) :: nml__status !< nml-tools status code
    character(len={{ spec.errmsg_len }}), intent(out) :: nml__errmsg !< error message for non-OK status values
    type({{ spec.type_name }}), pointer :: nml__obj
{% for decl in spec.bridge_declarations %}
{{ decl | indent(4, True) }}
{% endfor %}

    call {{ spec.resolve_handle_name }}(nml__handle, nml__obj, nml__status, nml__errmsg)
    if (.not. associated(nml__obj)) then
      return
    end if
{% for stmt in spec.bridge_assignments %}
{{ stmt | indent(4, True) }}
{% endfor %}
{% if spec.argument_list %}
    nml__status = nml__obj%set( &
{% for arg in spec.set_call_arguments %}
      {{ arg }}, &
{% endfor %}
      errmsg=nml__errmsg)
{% else %}
    nml__status = nml__obj%set(errmsg=nml__errmsg)
{% endif %}
  end subroutine {{ spec.namelist_name }}_set_wrapper

{% if spec.set_dims_argument_list %}
  !> \brief Set runtime dimensions on the handled {{ spec.namelist_name }} instance
  subroutine {{ spec.namelist_name }}_set_dims_wrapper(nml__handle, &
    {{ spec.set_dims_argument_list | join(', &\n    ') }}, &
    nml__status, nml__errmsg)
    integer(c_intptr_t), intent(in) :: nml__handle !< opaque handle to a {{ spec.type_name }} instance
{% for decl in spec.set_dims_argument_declarations %}
{{ decl | indent(4, True) }}
{% endfor %}
    integer, intent(out) :: nml__status !< nml-tools status code
    character(len={{ spec.errmsg_len }}), intent(out) :: nml__errmsg !< error message for non-OK status values
    type({{ spec.type_name }}), pointer :: nml__obj
{% for decl in spec.set_dims_bridge_declarations %}
{{ decl | indent(4, True) }}
{% endfor %}

    call {{ spec.resolve_handle_name }}(nml__handle, nml__obj, nml__status, nml__errmsg)
    if (.not. associated(nml__obj)) then
      return
    end if
{% for stmt in spec.set_dims_bridge_assignments %}
{{ stmt | indent(4, True) }}
{% endfor %}
    nml__status = nml__obj%set_dims( &
{% for arg in spec.set_dims_call_arguments %}
      {{ arg }}, &
{% endfor %}
      errmsg=nml__errmsg)
  end subroutine {{ spec.namelist_name }}_set_dims_wrapper

{% endif %}

  !> \brief Check whether a {{ spec.namelist_name }} field is set
  subroutine {{ spec.namelist_name }}_is_set_wrapper(nml__handle, name, idx__n1, idx, has__idx, nml__status, nml__errmsg)
    integer(c_intptr_t), intent(in) :: nml__handle !< opaque handle to a {{ spec.type_name }} instance
    character(len=*), intent(in) :: name !< field name
    integer, intent(in) :: idx__n1 !< extent for idx
    integer, dimension(idx__n1), intent(in) :: idx !< optional field index values
    logical, intent(in) :: has__idx !< whether idx was provided
    integer, intent(out) :: nml__status !< nml-tools status code
    character(len={{ spec.errmsg_len }}), intent(out) :: nml__errmsg !< error message for non-OK status values
    type({{ spec.type_name }}), pointer :: nml__obj
    integer, dimension(:), allocatable :: maybe__idx

    call {{ spec.resolve_handle_name }}(nml__handle, nml__obj, nml__status, nml__errmsg)
    if (.not. associated(nml__obj)) then
      return
    end if
    if (has__idx) then
      allocate(maybe__idx(idx__n1))
      maybe__idx = idx
    end if
    nml__status = nml__obj%is_set(name, idx=maybe__idx, errmsg=nml__errmsg)
  end subroutine {{ spec.namelist_name }}_is_set_wrapper

  !> \brief Validate the handled {{ spec.namelist_name }} instance
  subroutine {{ spec.namelist_name }}_is_valid_wrapper(nml__handle, nml__status, nml__errmsg)
    integer(c_intptr_t), intent(in) :: nml__handle !< opaque handle to a {{ spec.type_name }} instance
    integer, intent(out) :: nml__status !< nml-tools status code
    character(len={{ spec.errmsg_len }}), intent(out) :: nml__errmsg !< error message for non-OK status values
    type({{ spec.type_name }}), pointer :: nml__obj

    call {{ spec.resolve_handle_name }}(nml__handle, nml__obj, nml__status, nml__errmsg)
    if (.not. associated(nml__obj)) then
      return
    end if
    nml__status = nml__obj%is_valid(errmsg=nml__errmsg)
  end subroutine {{ spec.namelist_name }}_is_valid_wrapper

end module {{ spec.f2py_module_name }}

{% endfor %}
