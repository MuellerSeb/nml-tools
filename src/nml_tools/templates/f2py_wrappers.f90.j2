! This file was generated by nml-tools.
!> \file {{ file_name }}
{% for spec in specs %}
!> \copydoc {{ spec.f2py_module_name }}
{% endfor %}

{% for spec in specs %}
!> \brief {{ spec.brief }}
!> \details {{ spec.details_lines[0] }}
{% for line in spec.details_lines[1:] %}
!! {{ line }}
{% endfor %}
module {{ spec.f2py_module_name }}
  use iso_c_binding, only: c_intptr_t
{% if spec.kind_imports %}
  use {{ spec.kind_module }}, only: &
    {{ spec.kind_imports | join(', &\n    ') }}
{% endif %}
  use {{ spec.module_name }}, only: {{ spec.type_name }}, {{ spec.resolve_handle_name }}

  implicit none

contains

  !> \brief Read the {{ spec.namelist_name }} namelist from a file
  subroutine {{ spec.namelist_name }}_from_file_wrapper(handle, file, status, errmsg)
    integer(c_intptr_t), intent(in) :: handle !< opaque handle to a {{ spec.type_name }} instance
    character(len=*), intent(in) :: file !< namelist file path
    integer, intent(out) :: status !< nml-tools status code
    character(len={{ spec.errmsg_len }}), intent(out) :: errmsg !< error message for non-OK status values
    type({{ spec.type_name }}), pointer :: this

    call {{ spec.resolve_handle_name }}(handle, this, status, errmsg)
    if (.not. associated(this)) then
      return
    end if
    status = this%from_file(file, errmsg=errmsg)
  end subroutine {{ spec.namelist_name }}_from_file_wrapper

  !> \brief Set {{ spec.namelist_name }} values on the handled instance
  subroutine {{ spec.namelist_name }}_set_wrapper(handle, &
{% if spec.argument_list %}
    {{ spec.argument_list | join(', &\n    ') }}, &
{% endif %}
    status, errmsg)
    integer(c_intptr_t), intent(in) :: handle !< opaque handle to a {{ spec.type_name }} instance
{% for decl in spec.argument_declarations %}
{{ decl | indent(4, True) }}
{% endfor %}
    integer, intent(out) :: status !< nml-tools status code
    character(len={{ spec.errmsg_len }}), intent(out) :: errmsg !< error message for non-OK status values
    type({{ spec.type_name }}), pointer :: this
{% for decl in spec.bridge_declarations %}
{{ decl | indent(4, True) }}
{% endfor %}

    call {{ spec.resolve_handle_name }}(handle, this, status, errmsg)
    if (.not. associated(this)) then
      return
    end if
{% for stmt in spec.bridge_assignments %}
{{ stmt | indent(4, True) }}
{% endfor %}
{% if spec.argument_list %}
    status = this%set( &
{% for arg in spec.set_call_arguments %}
      {{ arg }}, &
{% endfor %}
      errmsg=errmsg)
{% else %}
    status = this%set(errmsg=errmsg)
{% endif %}
  end subroutine {{ spec.namelist_name }}_set_wrapper

{% if spec.set_constants_argument_list %}
  !> \brief Set runtime constants on the handled {{ spec.namelist_name }} instance
  subroutine {{ spec.namelist_name }}_set_constants_wrapper(handle, &
    {{ spec.set_constants_argument_list | join(', &\n    ') }}, &
    status, errmsg)
    integer(c_intptr_t), intent(in) :: handle !< opaque handle to a {{ spec.type_name }} instance
{% for decl in spec.set_constants_argument_declarations %}
{{ decl | indent(4, True) }}
{% endfor %}
    integer, intent(out) :: status !< nml-tools status code
    character(len={{ spec.errmsg_len }}), intent(out) :: errmsg !< error message for non-OK status values
    type({{ spec.type_name }}), pointer :: this
{% for decl in spec.set_constants_bridge_declarations %}
{{ decl | indent(4, True) }}
{% endfor %}

    call {{ spec.resolve_handle_name }}(handle, this, status, errmsg)
    if (.not. associated(this)) then
      return
    end if
{% for stmt in spec.set_constants_bridge_assignments %}
{{ stmt | indent(4, True) }}
{% endfor %}
    status = this%set_constants( &
{% for arg in spec.set_constants_call_arguments %}
      {{ arg }}, &
{% endfor %}
      errmsg=errmsg)
  end subroutine {{ spec.namelist_name }}_set_constants_wrapper

{% endif %}

  !> \brief Check whether a {{ spec.namelist_name }} field is set
  subroutine {{ spec.namelist_name }}_is_set_wrapper(handle, name, idx_n1, idx, has_idx, status, errmsg)
    integer(c_intptr_t), intent(in) :: handle !< opaque handle to a {{ spec.type_name }} instance
    character(len=*), intent(in) :: name !< field name
    integer, intent(in) :: idx_n1 !< extent for idx
    integer, dimension(idx_n1), intent(in) :: idx !< optional field index values
    logical, intent(in) :: has_idx !< whether idx was provided
    integer, intent(out) :: status !< nml-tools status code
    character(len={{ spec.errmsg_len }}), intent(out) :: errmsg !< error message for non-OK status values
    type({{ spec.type_name }}), pointer :: this
    integer, dimension(:), allocatable :: maybe_idx

    call {{ spec.resolve_handle_name }}(handle, this, status, errmsg)
    if (.not. associated(this)) then
      return
    end if
    if (has_idx) then
      allocate(maybe_idx(idx_n1))
      maybe_idx = idx
    end if
    status = this%is_set(name, idx=maybe_idx, errmsg=errmsg)
  end subroutine {{ spec.namelist_name }}_is_set_wrapper

  !> \brief Validate the handled {{ spec.namelist_name }} instance
  subroutine {{ spec.namelist_name }}_is_valid_wrapper(handle, status, errmsg)
    integer(c_intptr_t), intent(in) :: handle !< opaque handle to a {{ spec.type_name }} instance
    integer, intent(out) :: status !< nml-tools status code
    character(len={{ spec.errmsg_len }}), intent(out) :: errmsg !< error message for non-OK status values
    type({{ spec.type_name }}), pointer :: this

    call {{ spec.resolve_handle_name }}(handle, this, status, errmsg)
    if (.not. associated(this)) then
      return
    end if
    status = this%is_valid(errmsg=errmsg)
  end subroutine {{ spec.namelist_name }}_is_valid_wrapper

end module {{ spec.f2py_module_name }}

{% endfor %}
